import HelpClient from './HelpClient';

export const metadata = {
  title: 'Pusat Bantuan & FAQ Gamer — NexaPay',
  description: 'Panduan lengkap cara top-up, cek User ID, metode pembayaran QRIS/E-Wallet, dan solusi kendala transaksi di NexaPay.',
};

export default function HelpPage() {
  return <HelpClient />;
}
